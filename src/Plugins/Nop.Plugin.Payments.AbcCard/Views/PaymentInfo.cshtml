@{
    Layout = "";
}
@model Nop.Plugin.Payments.AbcCard.Models.PaymentInfoModel
<script>
    $(document).on('submit', '#co-payment-info-form', function (event) {
        event.preventDefault();
    });
</script>
<table width="100%" cellpadding="0" cellspacing="0">
    <tr>
        <td>
            @Html.Raw(Model.DescriptionText)
        </td>
    </tr>
    <tr>
        <td>
            <nop-label asp-for="CardNumber" />
        </td>
        <td>
            <nop-editor asp-for="CardNumber" />
            <span asp-validation-for="CardNumber"></span>
        </td>
    </tr>
</table>
